# 鸽鸽XD的小站项目自述

最后更新：2026-06-20 14:42

这份文件给以后接手本项目的 AI 或协作者阅读。聊天记录可能丢失，但项目约定和当前状态要写在这里。

## 项目基本信息

- 项目类型：Hexo 静态博客
- 当前本地目录：`/Users/gegexd/Documents/鸽鸽XD的小站 2`
- Git 远端：`ssh://git@ssh.github.com:443/GGXD1122/ggxd.git`
- 源码分支：`source`
- 部署分支：`master`
- 线上站点：`https://gegexd.com`
- 当前主题：`themes/archer`

## 每次修改后的固定要求

1. 修改项目后，要同步更新本文件，记录做了什么、为什么做、是否发布。
2. 发布或上传完成后，要停止相关后台进程，尤其是 Hexo 预览服务、构建/部署残留进程。
3. 不要依赖聊天记录保存项目约定；重要约定都要写进本文件。
4. 动手前先看相关源码和本文档，避免覆盖用户已有内容。

## AI 标志约定

- 如果文章主要由 AI 从零撰写，在文章 front matter 中加入：

```yaml
ai: true
```

- 如果文章是用户自己写的，AI 只是修改、排版、修错或发布，不要添加 `ai: true`。
- 如果一篇文章是混合情况，按主要作者判断；不确定就先问用户。

## 常用命令

优先使用项目自带脚本：

```bash
tools/check.sh
tools/publish.sh "提交信息"
tools/cleanup.sh
```

`tools/check.sh` 会执行 Hexo clean/generate，并检查核心生成文件和资源版本。

`tools/publish.sh` 会先检查，再提交源码、推送 `source` 分支、部署生成站点到 `master` 分支。

`tools/cleanup.sh` 用来停止本地 Hexo 预览和相关残留进程。每次发布完成后都要运行。

如果系统没有全局 Node/npm，可以使用 Codex 自带 Node 路径：

```bash
PATH="/Users/gegexd/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" tools/check.sh
```

## 发布注意事项

- Baidu 主动推送可能返回 `401 site error`，这不代表 GitHub Pages 部署失败。
- 发布后线上页面可能短暂 404，通常等几十秒再请求即可。
- 发布完成后用 `curl -I https://gegexd.com/posts/xxx.html` 验证线上状态。
- 如果开过本地预览，发布后必须停止预览进程。

## 最近操作记录

### 2026-06-20

- 从旧项目目录 `/Users/gegexd/Documents/GeGeXD.com/github` 拉取/迁移到当前目录。
- 旧项目目录已移到废纸篓：`/Users/gegexd/.Trash/github-old-codex-project-20260620-143230`。
- 修复 `tools/check.sh`、`tools/cleanup.sh`、`tools/publish.sh` 的执行权限，使文档中的直接运行方式可用。
- 新增测试文章 `source/_posts/0620-test.md`，该文章由 AI 编写，所以包含 `ai: true`。
- 已发布测试页：`https://gegexd.com/posts/test-page.html`，线上已验证 `HTTP 200`。
- 新增本自述文件，用来保存项目约定，防止聊天记录丢失后难以接手。
